# X-UI-Telegram-Bot
🤖 ربات تلگرام استعلام هوشمند پنل X-UI (نسخه سنایی)
این پروژه یک ربات تلگرام پیشرفته و بهینه برای مدیریت و استعلام وضعیت کانفیگ‌های پنل X-UI (نسخه MHSanaei) است که با زبان PHP توسعه یافته است.

🌟 ویژگی‌های کلیدی:
سیستم کوکی هوشمند: لاگین خودکار و ذخیره کوکی برای کاهش فشار روی پنل و سرعت بالای پاسخ‌دهی.

پشتیبانی از پروتکل‌های متنوع: امکان استعلام کانفیگ‌های Vless، Vmess و Trojan.

نمایش دقیق وضعیت: محاسبه هوشمند وضعیت (فعال/غیرفعال) بر اساس حجم مصرفی و تاریخ انقضا.

دکمه‌های رنگی شیشه‌ای: استفاده از جدیدترین آپدیت تلگرام برای دکمه‌های استایل‌دار (سبز، قرمز، آبی).

مدیریت چند پنل: قابلیت اضافه کردن، ویرایش و حذف چندین سرور مختلف به صورت همزمان.

پشتیبانی مستقیم: دکمه تماس با پشتیبانی با قابلیت تنظیم آیدی از داخل پنل مدیریت.

🚀 آموزش راه‌اندازی روی هاست
برای راه‌اندازی این ربات، مراحل زیر را دنبال کنید:

۱. پیش‌نیازها
هاست با پشتیبانی از PHP 7.4 به بالا.

دیتابیس MySQL.

گواهی SSL فعال روی دامنه (برای وبهوک تلگرام).

۲. تنظیمات دیتابیس
وارد phpMyAdmin شوید و یک دیتابیس جدید بسازید.

فایل database.sql موجود در این مخزن را در دیتابیس خود Import کنید.

۳. تنظیمات فایل کد
فایل index.php را باز کرده و اطلاعات زیر را در ابتدای فایل جایگزین کنید:

$botToken: توکن ربات خود را از @BotFather بگیرید.

$adminId: آیدی عددی تلگرام خود را وارد کنید.

اطلاعات اتصال به دیتابیس (dbHost, dbName, dbUser, dbPass).

۴. ست کردن وبهوک (Webhook)
آدرس فایل خود را به صورت زیر در مرورگر باز کنید تا وبهوک فعال شود:
https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://your-domain.com/path/to/index.php

🛠 راهنمای اضافه کردن پنل به ربات
پس از ورود به بخش مدیریت ربات، دکمه ➕ افزودن پنل را بزنید. اطلاعات را باید دقیقاً با فرمت زیر ارسال کنید:

نام سرور|آدرس پنل|نام کاربری|رمز عبور

مثال:
آلمان|http://83.147.216.45:2097/amininews|admin|password123

🔗 آدرس‌های لاگین مورد حمایت ربات
ربات بسیار منعطف است و آدرس‌های زیر را به صورت خودکار شناسایی و اصلاح می‌کند:

آدرس‌های ساده: http://1.2.3.4:2053

آدرس‌های دارای مسیر (Path): http://1.2.3.4:2053/mysecret

آدرس‌های کپی شده از مرورگر: اگر به اشتباه آدرس کامل صفحه لاگین را کپی کنید (مانند http://1.2.3.4:2053/login) ربات خودش آن را اصلاح می‌کند.

آدرس‌های HTTPS: اگر پنل شما SSL دارد، آدرس را با https وارد کنید.

نکته: اگر پنل شما دارای Web Root Path است (مانند /amininews)، حتماً آن را در انتهای آدرس وارد کنید تا ربات بتواند به API متصل شود.

📊 نحوه استعلام توسط کاربر
کاربر با زدن دکمه 📊 استعلام حجم و زمان و ارسال کانفیگ خود، اطلاعات زیر را دریافت می‌کند:

نام کانفیگ: استخراج شده از ایمیل کلاینت.

وضعیت: (فعال یا غیرفعال) بر اساس محدودیت‌ها.

تاریخ انقضا: نمایش تاریخ یا عبارت "شروع از اولین اتصال".

جزئیات مصرف: میزان آپلود، دانلود و ترافیک کل باقی‌مانده.

دکمه پشتیبانی: هدایت مستقیم به آیدی تلگرام مدیریت.

⚖️ لایسنس
این پروژه تحت لایسنس MIT منتشر شده است. استفاده و تغییر در آن با ذکر منبع آزاد است.

کلام آخر
اگر این پروژه برای شما مفید بود، با زدن ستون ⭐ (Star) در بالای صفحه، از ما حمایت کنید!
